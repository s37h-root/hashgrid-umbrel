version: "3.7"

services:
  app:
    image: ghcr.io/s37h-root/hashgrid-bridge:v1.0.10
    build: .
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR:-./data}:/data
    environment:
      - DATA_DIR=/data
      - PORT=3000
